server:
  address: 0.0.0.0
  port: 8080

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/Activity-Tracking

    username:   root

    password:  root123

  jpa:
    show-sql: true

    generate-ddl: true

    hibernate:
      ddl-auto: update
    properties:

      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect